<script>
/*
<template>
  <div class="heroes">
    <div class="hero str">
      <template v-for="hero in result.constants.heroes" :key="hero.id">
        <div class="heroPick" v-if="hero.stats.primaryAttribute == 'str'">
          <HeroAvatar :heroId="hero.id" />
        </div>
      </template>
    </div>
    <div class="hero agi">
      <template v-for="hero in result.constants.heroes" :key="hero.id">
        <div
          class="heroPick"
          v-for="hero in result.constants.heroes"
          :key="hero.id"
        >
          <HeroAvatar
            v-if="hero.stats.primaryAttribute == 'agi'"
            :heroId="hero.id"
          />
        </div>
      </template>
    </div>
    <div class="hero int">
      <div
        class="heroPick"
        v-for="hero in result.constants.heroes"
        :key="hero.id"
      >
        <HeroAvatar
          v-if="hero.stats.primaryAttribute == 'int'"
          :heroId="hero.id"
        />
      </div>
    </div>
  </div>
</template>
*/
</script>
